  //accesores

  //join convierte en cadena de texto y se puede usar cualquier separador
  let variable= ["miau",2,3,4,5,1];
  document.write(variable+"</br>");
  let resultado= variable.join("</br> elemento:  ");
  document.write(resultado);

  //slice igual que el substring pero con iniciadores

  let resultado1 = variable.slice(0 , 2);
  document.write(variable);